---
phase: 07-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/ui.ts
  - src/components/Contact.astro
  - src/components/Footer.astro
  - src/pages/[lang]/index.astro
autonomous: true
requirements:
  - REQ-009
  - REQ-010
must_haves:
  truths:
    - User can see contact options with matching design from Goal
    - User can see the footer with proper layout from Goal
    - Both components support internationalization correctly
  artifacts:
    - path: "src/components/Contact.astro"
      provides: "Contact grid component"
    - path: "src/components/Footer.astro"
      provides: "Site footer component"
  key_links:
    - from: "src/pages/[lang]/index.astro"
      to: "src/components/Contact.astro"
      via: "imported and rendered"
    - from: "src/pages/[lang]/index.astro"
      to: "src/components/Footer.astro"
      via: "imported and rendered"
---

<objective>
Implement the Contact section and Footer based on the "Goal" design, completely replacing the old hardcoded versions.
Purpose: Complete the final sections of the main page with proper layout, design tokens, and internationalization.
Output: Two new fully styled components integrated into the main page.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/About.astro
@src/pages/[lang]/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Contact and Footer translations</name>
  <files>src/i18n/ui.ts</files>
  <action>
    Add the required translation keys to both `pt` and `en` blocks in `src/i18n/ui.ts`.
    
    For `pt`:
    - `contact.kicker`: 'Índice · Contato'
    - `contact.title.1`: 'Vamos construir'
    - `contact.title.2`: 'algo juntos.'
    - `contact.fastest`: 'Mais rápido'
    - `contact.anytime`: 'A qualquer momento'
    - `contact.professional`: 'Profissional'
    - `contact.code`: 'Código'
    - `footer.built_with`: '© 2026 · Construído com Astro e carinho'
    - `footer.designed_with`: 'Projetado com Claude Design'
    - `footer.last_refactor`: 'Último refatoramento · Abr 2026'
    - `footer.back_to_top`: 'Voltar ao topo ↑'

    For `en`:
    - `contact.kicker`: 'Index · Contact'
    - `contact.title.1`: 'Let\'s build'
    - `contact.title.2`: 'something together.'
    - `contact.fastest`: 'Fastest'
    - `contact.anytime`: 'Anytime'
    - `contact.professional`: 'Professional'
    - `contact.code`: 'Code'
    - `footer.built_with`: '© 2026 · Built with Astro & care'
    - `footer.designed_with`: 'Designed with Claude Design'
    - `footer.last_refactor`: 'Last refactor · Apr 2026'
    - `footer.back_to_top`: 'Back to top ↑'
  </action>
  <verify>
    <automated>grep -q "contact.fastest" src/i18n/ui.ts</automated>
  </verify>
  <done>Translation strings exist for both languages.</done>
</task>

<task type="auto">
  <name>Task 2: Create Contact Component</name>
  <files>src/components/Contact.astro</files>
  <action>
    Create a new Astro component `src/components/Contact.astro` matching the structure from `The Goal/app.jsx` (the `Contact` function) and the component pattern of `About.astro`.
    1. Import `useTranslations`. Accept a `lang` prop (default "pt").
    2. Start the markup with `<section class="section contact" id="contact">` and `<div class="container">`.
    3. Add a section header like `About.astro` (`.section-head`, `.section-meta`, `.num-sm`, `.section-title serif`). Use "05" for the number.
    4. Build the title using `{t("contact.title.1")}` and `{t("contact.title.2")}`. Make sure the second part is inside `<em class="quiet">` with a `<br/>` before it.
    5. Define the grid of 4 `a.contact-card` elements (WhatsApp, Email, LinkedIn, GitHub).
       - URLs: WhatsApp (`https://wa.me/5547991158071?text=Hello%20Jefferson!%20How%20are%20you%3F`), Email (`mailto:jefferson.caires@gmail.com`), LinkedIn (`https://www.linkedin.com/in/jefferson-caires/`), GitHub (`https://github.com/JeffSSC`).
    6. Add CSS to `<style>`. Copy `.contact-grid`, `.contact-card`, `.contact-label`, `.contact-sub`, and `.quiet` from `The Goal/index.html`. Add `.quiet { color: var(--muted); font-style: italic; }`. Ensure it mimics the global structure of other components.
  </action>
  <verify>
    <automated>grep -q "class=\"contact-grid\"" src/components/Contact.astro</automated>
  </verify>
  <done>Contact component replicates the Goal layout.</done>
</task>

<task type="auto">
  <name>Task 3: Create Footer Component</name>
  <files>src/components/Footer.astro</files>
  <action>
    Create `src/components/Footer.astro` following the `The Goal` HTML structure and CSS.
    1. Import `useTranslations`, accept `lang` prop.
    2. Build the markup inside `<footer class="footer">`.
       - `.footer-big.serif` with the name "Jefferson Silva Caires."
       - `.footer-row` with the 4 metadata spans/links: `{t("footer.built_with")}`, `{t("footer.designed_with")}`, `{t("footer.last_refactor")}`, and a back to top link `{t("footer.back_to_top")}` pointing to `#top` or the top of the page.
    3. Add CSS to `<style>`: copy `.footer`, `.footer-big`, `.footer-row` from `The Goal/index.html`.
  </action>
  <verify>
    <automated>grep -q "class=\"footer-big" src/components/Footer.astro</automated>
  </verify>
  <done>Footer component is styled and supports i18n.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate in Main Page</name>
  <files>src/pages/[lang]/index.astro</files>
  <action>
    1. Import `Contact` and `Footer` components at the top.
    2. In the template body, locate the hardcoded `<section id="contact" class="contact flex flex-col items-center">` at the end of the `#projects-contact-content` div.
    3. Remove the entire old `<section id="contact">` block.
    4. Inject `<Contact lang={lang} />` and `<Footer lang={lang} />` inside `#projects-contact-content`.
  </action>
  <verify>
    <automated>grep -q "<Footer lang={lang} />" src/pages/\[lang\]/index.astro</automated>
  </verify>
  <done>Components are rendered and the old hardcoded contact code is removed.</done>
</task>

</tasks>
