---
phase: 08-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Navigation.astro
  - src/components/Hero.astro
  - src/components/About.astro
  - src/components/Experience.astro
  - src/components/Projects.astro
  - src/components/TechStack.astro
  - src/components/TestimonialComponent.astro
  - src/components/Contact.astro
  - src/components/Footer.astro
  - src/pages/[lang]/index.astro
autonomous: true
requirements:
  - REQ-011
  - REQ-012
  - REQ-013
  - REQ-015
must_haves:
  truths:
    - User can view portfolio on mobile (320px+) with proper layout
    - User can view portfolio on tablet (720px+) with proper layout
    - User can view portfolio on desktop (860px+) with proper layout
    - Page loads with smooth animations
    - Animations respect reduced-motion preference
    - Theme toggle persists preference across sessions
  artifacts:
    - path: "All components"
      provides: "Responsive layouts at all breakpoints"
    - path: "src/pages/[lang]/index.astro"
      provides: "AOS animations and reduced-motion support"
    - path: "src/components/ThemeToggle.astro"
      provides: "localStorage persistence"
  key_links:
    - from: "Navigation.astro"
      to: "ThemeToggle.astro"
      via: "theme state persistence"
    - from: "index.astro"
      to: "AOS library"
      via: "data-aos attributes"
---

<objective>
Polish all sections for responsive design, animations, and performance optimization.
Purpose: Make the portfolio fully production-ready with smooth UX across all devices.
Output: Responsive components with proper animations and performance.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-polish/08-CONTEXT.md
@src/pages/[lang]/index.astro
@src/components/ThemeToggle.astro
@src/components/Navigation.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add responsive CSS to all components</name>
  <files>src/components/*.astro</files>
  <action>
    Audit and fix responsiveness in all component files. For each component:

    1. Navigation.astro: Ensure nav links collapse properly on mobile, add hamburger menu if needed
    2. Hero.astro: Ensure title scales with clamp(), buttons stack on mobile
    3. About.astro: Stack columns on mobile, sidebar scrolls on desktop
    4. Experience.astro: Timeline works on mobile (horizontal scroll or stacked)
    5. Projects.astro: Filter buttons and project cards grid on mobile
    6. TechStack.astro: 3-column to 2-column to 1-column on breakpoints
    7. TestimonialComponent.astro: Single column on mobile, navigation works
    8. Contact.astro: Grid of 4 becomes 2x2 on mobile
    9. Footer.astro: Stack metadata on mobile

    Use existing Tailwind responsive classes: `md:`, `lg:` prefixes.
    Ensure minimum touch target 44px.
  </action>
  <verify>
    <automated>grep -E "md:|lg:" src/components/*.astro | wc -l</automated>
  </verify>
  <done>All components have responsive layouts at 520px, 720px, 860px breakpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and enhance animations</name>
  <files>src/pages/[lang]/index.astro</files>
  <action>
    1. Check existing AOS usage in index.astro
    2. Add data-aos="fade-up" to sections missing animations:
       - Hero section (fade-in)
       - Contact section (fade-up)
       - Footer (fade-in)
    3. Add reduced-motion support:
       - Import AOS styles
       - Add CSS: @media (prefers-reduced-motion: reduce) { [data-aos], [data-aos] * { transition: none !important; } }
    4. Test animations trigger properly on scroll
  </action>
  <verify>
    <automated>grep -q "prefers-reduced-motion" src/pages/\[lang\]/index.astro</automated>
  </verify>
  <done>AOS animations trigger on scroll, reduced-motion respected.</done>
</task>

<task type="auto">
  <name>Task 3: Polish theme toggle</name>
  <files>src/components/ThemeToggle.astro</files>
  <action>
    Inspect ThemeToggle.astro and ensure:
    1. Theme persists to localStorage key (check for localStorage.setItem)
    2. On page load, reads from localStorage first, then system preference
    3. Smooth CSS transitions between themes (transition: background-color 0.3s)
    4. No flash of wrong theme on load (inline script in head)
  </action>
  <verify>
    <automated>grep -q "localStorage" src/components/ThemeToggle.astro</automated>
  </verify>
  <done>Theme persists across sessions, no theme flash on load.</done>
</task>

<task type="auto">
  <name>Task 4: Performance audit and fixes</name>
  <files>src/pages/[lang]/index.astro</files>
  <action>
    Check and optimize:
    1. Verify no unused JavaScript bundles
    2. Check image optimization (astro Image or external URLs)
    3. Verify static generation (no client:load unless needed)
    4. Check for unnecessary client-side libraries
    5. Run build: npm run build and check for warnings
    6. For Credly embeds, consider lazy loading with loading="lazy"
  </action>
  <verify>
    <automated>cd /home/jeffsc/astro_portfolio && npm run build 2>&1 | head -30</automated>
  </verify>
  <done>Performance optimized, build passes.</done>
</task>

</tasks>

<verification>
All components render without errors, responsive at all breakpoints
</verification>

<success_criteria>
- Build completes without errors
- Responsive layouts at 520px, 720px, 860px
- Reduced motion supported
- Theme persists across sessions
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish/08-01-SUMMARY.md`
</output>