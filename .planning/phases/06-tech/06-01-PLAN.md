---
phase: 06-tech
plan: 01
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - src/components/TechStack.astro
  - src/components/TestimonialComponent.astro
  - src/pages/[lang]/index.astro
autonomous: true
requirements: [REQ-006, REQ-008]
must_haves:
  truths:
    - "User can view tech stack organized by categories (Frontend, Backend, Tools)"
    - "User can see certifications displayed"
    - "User can browse testimonials with navigation"
    - "All content uses i18n (en/pt)"
    - "Styling matches design system (no Tailwind, scoped CSS)"
  artifacts:
    - path: "src/components/TechStack.astro"
      provides: "Tech stack grid with categories and certifications"
      style: "scoped CSS with design tokens"
    - path: "src/components/TestimonialComponent.astro"
      provides: "Testimonial carousel with navigation"
      style: "scoped CSS with design tokens"
    - path: "src/pages/[lang]/index.astro"
      provides: "Integration of TechStack and Testimonials sections"
  key_links:
    - from: "src/components/TechStack.astro"
      to: "src/i18n/ui.ts"
      via: "useTranslations import"
    - from: "src/components/TestimonialComponent.astro"
      to: "src/i18n/ui.ts"
      via: "useTranslations import"
    - from: "src/pages/[lang]/index.astro"
      to: "src/components/TechStack.astro"
      via: "import and lang prop"
---

<objective>
Implement Tech Stack and Testimonials sections following the design system patterns from About.astro and Experience.astro.

Purpose: Add remaining portfolio sections with proper styling and i18n support
Output: Two new components and page integration
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md (REQ-006, REQ-008)
@src/components/About.astro (design pattern)
@src/components/Experience.astro (design pattern)
@src/components/TechStack.astro (existing, needs refactor)
@src/components/TestimonialComponent.astro (existing, needs refactor)
@src/i18n/ui.ts (content)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor TechStack component</name>
  <files>src/components/TechStack.astro</files>
  <action>
Refactor TechStack.astro to match design system:

1. Add import: `import { useTranslations } from "../i18n/utils";`
2. Add Props interface with `lang?: string` defaulting to "pt"
3. Add section header with section number (03 or 04), title "Technologies" from i18n (`t("tech.title")`)
4. Structure with three category columns: Frontend, Backend, Tools
5. Use existing tech items from ui.ts: astro, vuejs, python, html5, css3, git, github, aws, docker, nodejs, tailwindcss, postgresql, mysql
6. Add certifications row below: AWS AI Practitioner, AWS Cloud Practitioner
7. Replace Tailwind classes with scoped CSS using design tokens (--bg, --ink, --rule, --card, --accent, --font-serif, --font-sans, --font-mono)
8. Match section layout from Experience.astro: section-head with grid, section-meta with number + title

Tech items should display as icon + label format similar to existing implementation.
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -20</automated>
  </verify>
  <done>TechStack component has section header, three category columns, certifications row, uses scoped CSS with design tokens</done>
</task>

<task type="auto">
  <name>Task 2: Refactor TestimonialComponent</name>
  <files>src/components/TestimonialComponent.astro</files>
  <action>
Refactor TestimonialComponent.astro to match design system:

1. Add import: `import { useTranslations } from "../i18n/utils";`
2. Add Props interface with `lang?: string` defaulting to "pt"
3. Get testimonials from i18n: `const testimonials = t("testimonials") as any;`
4. Replace Tailwind classes with scoped CSS:
   - Container: max-width 1320px, centered, padding
   - Cards: use --card background, --rule border, border-radius
   - Text: --ink-2 color, --font-serif for quotes
   - Avatar: circular, 56px
   - Author: --ink color, --font-sans
   - Role: --muted color, --font-sans, 14px
5. Keep Swiper JS for carousel functionality (prev/next buttons)
6. Add section header matching other sections: number + title "Testimonials" from i18n (`t("testimonials.title")`)
7. Style navigation buttons to match theme (no Tailwind, use CSS variables)
8. Ensure responsive: stack on mobile (<720px)
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -20</automated>
  </verify>
  <done>TestimonialComponent has section header, carousel with navigation, scoped CSS, i18n support</done>
</task>

<task type="auto">
  <name>Task 3: Integrate components into page</name>
  <files>src/pages/[lang]/index.astro</files>
  <action>
Integrate TechStack and TestimonialComponent into the main page:

1. Import both components at the top:
   ```astro
   import TechStack from "../../components/TechStack.astro";
   import TestimonialComponent from "../../components/TestimonialComponent.astro";
   ```

2. Add components to the page after Projects section:
   - `<TechStack lang={lang} />`
   - `<TestimonialComponent lang={lang} />`

3. Ensure both components receive the `lang` prop for i18n

4. Verify section IDs: TechStack should have `id="stack"` or `id="tech"`, Testimonials should have `id="testimonials"`
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -20</automated>
  </verify>
  <done>Both components rendered on page with proper lang prop</done>
</task>

</tasks>

<verification>
[Overall phase checks]
- Build passes without errors
- Both sections visible on page (Tech Stack, Testimonials)
- i18n works (switching en/pt changes content)
- No Tailwind classes in component files
- Scoped CSS uses design tokens correctly
</verification>

<success_criteria>
- TechStack displays tech items in category columns with icons
- Certifications row shows AWS credentials
- Testimonials carousel works with prev/next navigation
- Both sections use i18n (en/pt)
- Styling matches About/Experience design pattern
- Navigation links work to #tech and #testimonials
</success_criteria>

<output>
After completion, create `.planning/phases/06-tech/06-01-SUMMARY.md`
</output>