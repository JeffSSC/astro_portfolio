---
phase: 03-hero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Hero.astro
  - src/components/About.astro
  - src/pages/[lang]/index.astro
autonomous: true
requirements:
  - REQ-003
  - REQ-004
must_haves:
  truths:
    - User sees hero section with name, role, tagline on page load
    - User can click CTA buttons to navigate to sections
    - User can see availability indicator (green dot)
    - User sees bio text in two-column layout on desktop
    - User sees key-value sidebar sticky on scroll
  artifacts:
    - path: src/components/Hero.astro
      provides: Full viewport hero section
    - path: src/components/About.astro
      provides: Bio + sidebar section
  key_links:
    - from: src/pages/[lang]/index.astro
      to: src/components/Hero.astro
      via: import and usage
    - from: src/pages/[lang]/index.astro
      to: src/components/About.astro
      via: import and usage
---

<objective>
Create Hero and About sections matching "The Goal" reference design.

Purpose: Implement the two main content sections with proper typography, layout, and animations per reference spec.

Output: Working Hero.astro and About.astro components
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hero/03-CONTEXT.md

# Reference design from The Goal (React patterns being converted)
@The Goal/index.html (lines 116-216: Hero and About sections)

# Existing components to reference
@src/styles/design-tokens.css (CSS variables - already created)
@src/components/Navigation.astro (existing from Phase 2)
@src/components/ThemeToggle.astro (existing from Phase 1)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero Component</name>
  <files>src/components/Hero.astro</files>
  <read_first>
- src/styles/design-tokens.css (for CSS variables usage)
- src/i18n/ui.ts (for translations)
- src/components/Navigation.astro (for anchor patterns)
  </read_first>
  <action>
Create `src/components/Hero.astro` with:

1. **Structure:**
```astro
<section class="hero" id="hero">
  <!-- Grid dot background -->
  <div class="hero-grid-bg"></div>
  
  <!-- Top row: meta + availability -->
  <div class="hero-top">
    <div class="hero-meta">
      <span class="mono text-sm">Based in Brazil</span>
    </div>
    <div class="hero-meta right">
      <span class="mono text-sm">Available for work</span>
      <div class="dot"></div>
    </div>
  </div>
  
  <!-- Main title -->
  <h1 class="hero-title serif">
    Jefferson Silva Caires
  </h1>
  
  <!-- Bottom: role + tagline + CTAs -->
  <div class="hero-bottom">
    <div class="hero-role">
      <span class="role-text">Full-Stack Developer</span>
    </div>
    <p class="hero-tagline">
      Building digital products with clean code and thoughtful design.
    </p>
    <div class="hero-cta">
      <a href="#work" class="btn-primary">View Work</a>
      <a href="#contact" class="btn-ghost">Contact Me</a>
    </div>
  </div>
</section>
```

2. **CSS Styles (scoped to component):**
- `.hero`: min-height 88vh, position relative, flex column, justify-content space-between
- `.hero-grid-bg`: absolute inset 0, CSS grid pattern (1px lines at 80px intervals), opacity 0.3
- `.hero-title`: clamp(80px, 16vw, 240px), line-height 0.92, letter-spacing -0.04em
- `.dot`: 8px circle, accent color, pulse animation (2.4s infinite)
- `.btn-primary`: ink bg, white text, hover accent bg
- `.btn-ghost`: border 1px rule, hover fills card

3. **Use content from i18n/ui.ts:**
- Title: "{hero.name}" from translations
- Role: "{hero.role}" from translations
- CTA buttons: Use translation keys or hardcode for now

  </action>
  <verify>
    <automated>grep -q "class=\"hero\"" src/components/Hero.astro && grep -q "class=\"hero-title\"" src/components/Hero.astro && echo "Hero component structure: PASS"</automated>
  </verify>
  <done>
Hero renders with: title "Jefferson Silva Caires", role, tagline, CTAs, availability dot, fade-in animation</done>
</task>

<task type="auto">
  <name>Task 2: Create About Component</name>
  <files>src/components/About.astro</files>
  <read_first>
- src/styles/design-tokens.css
- src/i18n/ui.ts (about.text paragraphs)
- src/components/Hero.astro (for layout patterns)
  </read_first>
  <action>
Create `src/components/About.astro` with:

1. **Structure:**
```astro
---
import { useTranslations } from "../i18n/utils";

const lang = /* get from page */ "pt";
const t = useTranslations(lang);
const aboutText = t("about.text") || [];
---
<section class="section about" id="about">
  <div class="container">
    <div class="section-head">
      <span class="section-meta">
        <span class="num-sm mono">01</span>
        <span class="section-title serif">About</span>
      </span>
    </div>
    
    <div class="about-grid">
      <div class="about-main">
        {aboutText.map(( paragraph) => (
          <p class="about-p serif" set:html={paragraph} />
        ))}
      </div>
      
      <aside class="about-side">
        <dl class="kv">
          <dt class="mono text-xs">Location</dt>
          <dd>Brazil</dd>
          
          <dt class="mono text-xs">Availability</dt>
          <dd>Open to work</dd>
          
          <dt class="mono text-xs">Role</dt>
          <dd>Full-Stack Developer</dd>
        </dl>
      </aside>
    </div>
  </div>
</section>
```

2. **CSS Styles:**
- `.about-grid`: display grid, columns 1fr 360px, gap 60px
- `.about-p`: serif font, 28px, line-height 1.32, margin-bottom 18px
- `.about-side`: position sticky, top 100px
- `.kv`: grid columns 90px 1fr, gap 14px 20px, border 1px card, padding 24px

  </action>
  <verify>
    <automated>grep -q "class=\"about\"" src/components/About.astro && grep -q "class=\"about-grid\"" src/components/About.astro && echo "About component structure: PASS"</automated>
  </verify>
  <done>
About renders with: two-column layout, bio paragraphs, key-value sidebar, sticky behavior</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Hero and About into Page</name>
  <files>src/pages/[lang]/index.astro</files>
  <read_first>
- src/pages/[lang]/index.astro (existing structure)
- src/components/Hero.astro (to integrate)
- src/components/About.astro (to integrate)
  </read_first>
  <action>
Update `src/pages/[lang]/index.astro`:

1. **Add imports:**
```astro
import Hero from "../components/Hero.astro";
import About from "../components/About.astro";
```

2. **Remove old landing/about sections:**
- Delete the existing `#landing` section with HomeAnimation, ScrambleText
- Delete the existing `#about` section with SectionTitle, Text components

3. **Add new sections:**
```astro
<PageLayout title={pageTitle} description={pageDescription}>
    <Hero />
    
    <div id="main-content">
        <About />
        
        <!-- Keep existing: experience, stack, testimonials, work, contact -->
        <section id="experience" ...
        <section id="stack" ...
        <section id="testimonials" ...
        <section id="work" ...
        <section id="contact" ...
    </div>
</PageLayout>
```

4. **Update i18n if needed:**
- Ensure about.text has 2-3 paragraphs for proper two-column layout

  </action>
  <verify>
    <automated>grep -q "import Hero" src/pages/\[lang\]/index.astro && grep -q "import About" src/pages/\[lang\]/index.astro && echo "Imports added: PASS"</automated>
  </verify>
  <done>
Page renders with Hero section first, then About section with anchor ID #about</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server | No external input - static Astro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Hero/About | accept | Static content, no user input |
</threat_model>

<verification>
[ ] Build passes: npm run build
[ ] Hero section visible on page load
[ ] About section at #about anchor
[ ] CTAs link to #work and #contact
[ ] Responsive layout works
</verification>

<success_criteria>
Hero and About sections render with correct styling, content loads from i18n, CTAs navigate to sections, layout responsive on desktop/mobile
</success_criteria>

<output>
After completion, create `.planning/phases/03-hero/03-01-SUMMARY.md`
</output>