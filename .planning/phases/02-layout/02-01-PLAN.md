---
phase: "02-layout"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/pages/[lang]/index.astro"
  - "src/layouts/PageLayout.astro"
autonomous: true
requirements:
  - "REQ-002"
  - "REQ-014"
must_haves:
  truths:
    - "User can navigate to any section via anchor links"
    - "Navigation shows scroll state when scrolled"
    - "Language can be switched and persists across sessions"
    - "Mobile shows only brand, desktop shows full nav"
  artifacts:
    - path: "src/components/Navigation.astro"
      provides: "Sticky navigation with blur backdrop, brand mark, nav links, theme toggle"
      contains: ".nav, .nav-scrolled, .nav-brand, .nav-links, .nav-link"
    - path: "src/components/LanguageSwitcher.astro"
      provides: "EN/PT language toggle with localStorage persistence"
      contains: ".lang-switch, localStorage read/write"
  key_links:
    - from: "Navigation.astro"
      to: "ThemeToggle.astro"
      via: "import"
      pattern: "ThemeToggle"
    - from: "Navigation.astro"
      to: "PageLayout.astro"
      via: "slot"
      pattern: "slot"
---

<objective>
Create navigation component and language switcher based on The Goal reference design.

Purpose: Implement sticky navigation with blur backdrop, brand mark, anchor links to sections, language switcher with localStorage persistence, and scroll state detection.

Output: Working Navigation component integrated into page layout with smooth scroll functionality.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout/02-CONTEXT.md

# Reference navigation styles from The Goal
@The Goal/styles.css (lines 64-114)

# Existing theme implementation from Phase 1
@src/components/ThemeToggle.astro
@src/stores/theme.ts

# Current PageLayout
@src/layouts/PageLayout.astro

# Current index page (will receive anchor IDs)
@src/pages/[lang]/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Navigation Component</name>
  <files>src/components/Navigation.astro</files>
  <action>
Create Navigation.astro component with:

1. **Structure**: Sticky nav with 3-column grid (brand | links | right controls)
2. **Brand Mark**: "J" monogram in serif font with border (34x34px)
3. **Nav Links**: About, Experience, Work, Contact → #about, #experience, #work, #contact
4. **Right Controls**: Language switcher + Theme toggle
5. **Scroll State**: JavaScript to toggle .nav-scrolled class on scroll
6. **Progress Bar**: Optional progress indicator (width scales with scroll)

**CSS from Reference** (lines 64-114 in The Goal/styles.css):
- Use .nav, .nav-scrolled, .nav-brand, .brand-mark, .nav-links, .nav-link classes
- Mobile: <720px hide nav-links, show brand only
- Hover: underline animation via ::after pseudo-element

**Implementation Details**:
- Import ThemeToggle from existing component
- Import LanguageSwitcher from task 2
- Use inline script for scroll detection (no external JS file needed)
- Apply backdrop-filter: blur(14px) with 85% opacity background
- Border appears on scroll via .nav-scrolled class

**Avoid**: Adding new external dependencies. Use vanilla JS for scroll detection.
  </action>
  <verify>
<automated>npm run build 2>&1 | head -30</automated>
  </verify>
  <done>
Navigation component renders with sticky positioning, brand mark shows "J", nav links present on desktop, scroll state toggles border, build passes without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Language Switcher Component</name>
  <files>src/components/LanguageSwitcher.astro</files>
  <action>
Create LanguageSwitcher.astro with:

1. **UI**: EN | PT toggle buttons in mono font
2. **State**: Read from localStorage on mount, default to "en"
3. **Behavior**: Click switches language, persists to localStorage
4. **Persist Key**: `lang` in localStorage

**Implementation**:
- Get current locale from page URL or localStorage
- Render two buttons: "EN" and "PT"
- Active button has .on class (darker color)
- Inline script to handle clicks: sets localStorage and optionally redirects
- For now, show both options - full i18n routing comes later (Phase 3+)

**Simplification**: Since Astro uses /en and /pt routes, this component just needs to show active state. Full route switching will be implemented in a later phase. Focus on visual toggle.

**Avoid**: Don't implement full routing yet - just the visual toggle matching reference design.
  </action>
  <verify>
<automated>npm run build 2>&1 | head -30</automated>
  </verify>
  <done>
Language switcher renders with EN/PT options, active state visible, component imports into Navigation without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate Navigation into Page Layout</name>
  <files>src/layouts/PageLayout.astro, src/pages/[lang]/index.astro</files>
  <action>
Update PageLayout.astro:

1. Import Navigation component
2. Add Navigation before main content slot
3. Wrap content in container div to offset sticky nav

Update src/pages/[lang]/index.astro:

1. Add anchor IDs to sections: #about, #experience, #work, #contact
2. These IDs will be targeted by nav links

**Implementation**:
- PageLayout: Import and render `<Navigation />` at top
- Index: Add id attributes to section containers:
  - About section → id="about"
  - Experience section → id="experience"  
  - Projects section → id="work"
  - Contact section → id="contact"

**Verify**: Navigation appears on page, clicking nav links scrolls to sections, smooth scroll works (already in base.css: scroll-behavior: smooth)
  </action>
  <verify>
<automated>npm run build 2>&1 | head -30</automated>
  </verify>
  <done>
Navigation integrated into layout, anchor IDs present on page sections, smooth scroll functional
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→client | JavaScript runs in browser for scroll detection, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | localStorage manipulation | accept | User can modify own localStorage, low risk |
| T-02-02 | Info Disclosure | URL reveals language | accept | Language in URL is intentional, not sensitive |
</threat_model>

<verification>
| Criterion | Check |
|-----------|-------|
| Build passes | npm run build succeeds |
| Navigation renders | Component exists and imports |
| Scroll state works | JS toggles .nav-scrolled on scroll |
| Anchor links work | Click scrolls to #section-id |
| Mobile hides links | CSS media query at 720px |
</verification>

<success_criteria>
- [x] Navigation component created in src/components/Navigation.astro
- [x] LanguageSwitcher component created
- [x] Navigation integrated into PageLayout
- [x] Anchor IDs added to sections
- [x] Sticky positioning with blur backdrop works
- [x] Scroll state detection functional
- [x] Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout/02-01-SUMMARY.md`
</output>