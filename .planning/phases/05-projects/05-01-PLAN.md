---
phase: "05-projects"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/[lang]/index.astro
autonomous: true
requirements:
  - REQ-007
must_haves:
  truths:
    - User can toggle between list and grid views
    - User can filter projects by category
    - Project cards show title, description, tags
    - Links to live demo and GitHub present
  artifacts:
    - path: src/components/Projects.astro
      provides: Projects listing with list/grid toggle, filtering
      contains: .proj-list, .proj-grid, .filter, .proj-card
---

<objective>
Create Projects section matching The Goal reference with list/grid toggle and filtering.

Purpose: Implement project cards with toggle view, category filters, and links per reference spec.

Output: Working Projects.astro component
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md (REQ-007)
@.planning/phases/05-projects/05-CONTEXT.md

# Reference design from The Goal (lines 290-358)
@The Goal/index.html (lines 290-358: Projects section)

# Existing
@src/i18n/ui.ts (project translations)
@src/components/Experience.astro (layout patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Projects Component</name>
  <files>src/components/Projects.astro</files>
  <read_first>
- src/styles/design-tokens.css
- src/i18n/ui.ts
- src/components/Experience.astro
  </read_first>
  <action>
Create `src/components/Projects.astro` with:

1. **Structure:**
```astro
---
import { useTranslations } from "../i18n/utils";

interface Props {
  lang?: string;
}

const { lang = "pt" } = Astro.props;
const t = useTranslations(lang as any);

const projects = [
  {
    title: t("proj.golden.title"),
    desc: t("proj.golden.desc"),
    tags: ["Python", "AWS", "QR Code"],
    category: "fullstack",
    link: "https://goldenparktag.com",
    github: "https://github.com/JeffSSC/goldenpark-tag",
  },
  // ... more projects
];
---

<section class="section projects" id="work">
  <div class="container">
    <div class="section-head">
      <span class="section-meta">
        <span class="num-sm mono">03</span>
        <h2 class="section-title serif">Work</h2>
      </span>
      <div class="filter-row">
        <button class="filter on" data-filter="all">All</button>
        <button class="filter" data-filter="frontend">Frontend</button>
        <button class="filter" data-filter="backend">Backend</button>
        <button class="filter" data-filter="fullstack">Full-Stack</button>
      </div>
    </div>
    
    <!-- Grid View (default) -->
    <div class="proj-grid">
      {projects.map((proj) => (
        <article class="proj-card">
          <h3 class="proj-card-title serif">{proj.title}</h3>
          <p class="proj-card-sum">{proj.desc}</p>
          <div class="chips">
            {proj.tags.map((tag) => (
              <span class="chip">{tag}</span>
            ))}
          </div>
        </article>
      ))}
    </div>
  </div>
</section>
```

2. **CSS Styles:**
- `.proj-grid`: grid columns repeat(2, 1fr), gap 2px, border 1px
- `.proj-card`: bg bg, padding 32px, flex column, gap 14px, min-height 320px
- `.filter-row`: display flex, gap 4px
- `.filter`: inline-flex, gap 8px, padding 8px 14px, border 1px
- `.filter.on`: bg ink, color bg

  </action>
  <verify>
<automated>grep -q "class=\"projects\"" src/components/Projects.astro && grep -q "class=\"proj-grid\"" src/components/Projects.astro && echo "Projects: PASS"</automated>
  </verify>
  <done>
Projects render with grid layout, filter buttons, project cards</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Projects into Page</name>
  <files>src/pages/[lang]/index.astro</files>
  <action>
1. **Add import:**
```astro
import Projects from "../../components/Projects.astro";
```

2. **Add after Experience** in main-content div:
```astro
<Experience lang={lang} />
<Projects lang={lang} />
```

3. **Remove old projects section** (old #work with CardProject components)

  </action>
  <verify>
<automated>grep -q "import Projects" src/pages/\[lang\]/index.astro && echo "Import: PASS"</automated>
  </verify>
  <done>
Projects section visible at #work anchor</done>
</task>

</tasks>

<verification>
[ ] Build passes
[ ] Projects visible at #work
[ ] Filter buttons work
[ ] Grid layout responsive
</verification>

<success_criteria>
Projects display with filtering, grid layout, follows design system
</success_criteria>

<output>
After completion, create `.planning/phases/05-projects/05-01-SUMMARY.md`
</output>