---
phase: "04-experience"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/[lang]/index.astro
autonomous: true
requirements:
  - REQ-005
must_haves:
  truths:
    - User sees experience entries in timeline format
    - Company name and role visible
    - Year badge displays prominently
    - Bullet points list achievements
  artifacts:
    - path: src/components/Experience.astro
      provides: Timeline-based experience entries
      contains: .exp-list, .exp-item, .company, .year
---

<objective>
Create Experience section matching The Goal reference design with timeline-based entries.

Purpose: Implement experience entries with year badges, company info, role, and achievement bullets per reference spec.

Output: Working Experience.astro component
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-experience/04-CONTEXT.md

# Reference design from The Goal (lines 217-234)
@The Goal/index.html (lines 217-234: Experience section)

# Existing components to reference
@src/styles/design-tokens.css
@src/i18n/ui.ts (experience translations)
@src/components/About.astro (layout patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Experience Component</name>
  <files>src/components/Experience.astro</files>
  <read_first>
- src/styles/design-tokens.css
- src/i18n/ui.ts
- src/components/About.astro
  </read_first>
  <action>
Create `src/components/Experience.astro` with:

1. **Structure:**
```astro
---
import { useTranslations } from "../i18n/utils";

interface Props {
  lang?: string;
}

const { lang = "pt" } = Astro.props;
const t = useTranslations(lang as any);

// Experience data from i18n
const experiences = [
  {
    company: "Compass UOL",
    role: t("exp.compass.role"),
    year: "2024",
    time: t("exp.compass.time"),
    modality: t("exp.compass.modality"),
    desc: [
      t("exp.compass.desc.1"),
      t("exp.compass.desc.2"),
      t("exp.compass.desc.3"),
      t("exp.compass.desc.4"),
    ],
  },
  {
    company: "Informatech",
    role: t("exp.informatech.role"),
    year: "2023",
    time: t("exp.informatech.time"),
    modality: t("exp.informatech.modality"),
    desc: [
      t("exp.informatech.desc.1"),
      t("exp.informatech.desc.2"),
      t("exp.informatech.desc.3"),
      t("exp.informatech.desc.4"),
    ],
  },
];
---
<section class="section experience" id="experience">
  <div class="container">
    <div class="section-head">
      <span class="section-meta">
        <span class="num-sm mono">02</span>
        <span class="section-title serif">Experience</span>
      </span>
    </div>
    
    <ul class="exp-list">
      {experiences.map((exp, index) => (
        <li class="exp-item">
          <div class="exp-year">
            <span class="big-year serif">{exp.year}</span>
            <span class="mono text-sm">{exp.time} · {exp.modality}</span>
          </div>
          <div class="exp-main">
            <div class="exp-head">
              <h3 class="exp-company serif">{exp.company}</h3>
              <span class="exp-role mono">{exp.role}</span>
            </div>
            <ul class="exp-bullets">
              {exp.desc.map((item) => (
                <li>
                  <span class="bullet-num mono">{String(index + 1).padStart(2, '0')}</span>
                  <span>{item}</span>
                </li>
              ))}
            </ul>
          </div>
        </li>
      ))}
    </ul>
  </div>
</section>
```

2. **CSS Styles:**
- `.exp-list`: list-style none, padding 0, margin 0, display grid, gap 60px
- `.exp-item`: grid columns 200px 1fr, gap 48px, padding-top 30px, border-top
- `.big-year`: font-size 72px, line-height 1, letter-spacing -0.03em
- `.exp-company`: font-size 40px, line-height 1.05
- `.exp-bullets`: grid columns 30px 1fr, gap 14px

  </action>
  <verify>
<automated>grep -q "class=\"experience\"" src/components/Experience.astro && grep -q "class=\"exp-list\"" src/components/Experience.astro && echo "Experience component: PASS"</automated>
  </verify>
  <done>
Experience renders: timeline layout, company names, roles, year badges, bullet points</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Experience into Page</name>
  <files>src/pages/[lang]/index.astro</files>
  <read_first>
- src/pages/[lang]/index.astro
- src/components/Experience.astro
  </read_first>
  <action>
Update `src/pages/[lang]/index.astro`:

1. **Add import:**
```astro
import Experience from "../../components/Experience.astro";
```

2. **Add Experience component** after About section in main-content div:
```astro
<About lang={lang} />
<Experience lang={lang} />
```

3. **Remove old experience section** (the existing #experience with ExpComponent)

  </action>
  <verify>
<automated>grep -q "import Experience" src/pages/\[lang\]/index.astro && echo "Experience import: PASS"</automated>
  </verify>
  <done>
Experience section integrated at #experience anchor</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server | No external input - static Astro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Experience | accept | Static content, no user input |
</threat_model>

<verification>
[ ] Build passes: npm run build
[ ] Experience section visible at #experience anchor
[ ] Timeline layout displays correctly
[ ] Responsive layout works
</verification>

<success_criteria>
Experience section renders with timeline layout, company names, roles, years, bullet points, follows design system
</success_criteria>

<output>
After completion, create `.planning/phases/04-experience/04-01-SUMMARY.md`
</output>